# Rolliúde Play AI Adapter

Use `AGENTS.md` and `project-standards/` as the mandatory source for repository work. This file intentionally delegates to the canonical standards to avoid duplicated rules.
