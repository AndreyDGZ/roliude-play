# Git And Documentation

Use Conventional Commits and focused pull requests.

Branches start from `develop`. Use `feature/`, `fix/`, `refactor/`, `docs/`, `test/` or `chore/`.

Update canonical documentation in the same pull request as the related structural change.

Historical documents must be labeled as historical before being used as reference material.
